# Required for authenticated HTTP MCP and instruction API deployments.
# Use a long random value and send it as: Authorization: Bearer <value>
MCP_API_KEY=

# Optional: increases GitHub API limits and enables private repositories.
GITHUB_TOKEN=

# Optional instruction API settings.
SKILLS_DIR=./skills
